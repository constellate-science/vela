"""Three minimal agents that exercise the v0.193-v0.195 substrate
through the vela_agent SDK. Each is runnable as a standalone script
against a fixture frontier (see the test gate
`scripts/test-agent-sdk.sh` for the wiring).
"""
