import Vela.Log

/-!
# Compatibility import for Vela canonical-order theorem modules

The current checked model for canonical finite-log replay lives in `Vela.Log`.
A future stronger development can replace this compatibility module with a full
canonical topological-sort proof over finite DAGs.
-/
