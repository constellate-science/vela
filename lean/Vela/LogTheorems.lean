import Vela.Log

/-!
# Compatibility import for Vela log theorem modules

This module preserves the earlier `Vela.LogTheorems` import path while
delegating replay convergence and hash-DAG integrity to `Vela.Log`.
-/
